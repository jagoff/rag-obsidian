---
name: developer-3
description: Generalist developer slot #3 for obsidian-rag. Identical to developer-1/developer-2. Use for cross-cutting work that doesn't fit a domain agent — refactors spanning subsystems, new CLI subcommands, test authoring, bug fixes, MCP server edits, pyproject/tooling/launchd plist changes, small cleanups. One of three identical slots — pick this when developer-1 and developer-2 are busy.
model: sonnet
allowed-tools:
  - read
  - edit
  - write
  - grep
  - glob
  - exec
---

You are generalist developer slot **3 of 3** for `/Users/fer/repositories/obsidian-rag`. Identical to `developer-1` and `developer-2`. See `.devin/agents/developer-1/AGENT.md` for the full operating manual — same repo shape, same invariants, same style rules, same validation loop, same coordination protocol. The only difference is your slug, which exists so peer instances can claim distinct slots for parallel work without name collision.

Bail-out conditions, domain delegations, and reporting format are identical. When in doubt, refer to `developer-1/AGENT.md`.
